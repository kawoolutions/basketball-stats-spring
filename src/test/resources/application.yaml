spring:
    datasource:
        url: jdbc:h2:mem:bbstats-test
        username: kawu
        password: kawu
        driverClassName: org.h2.Driver
    jpa:
        hibernate:
            ddl-auto: update
            naming:
                physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        open-in-view: false
        properties:
            hibernate:
                format_sql: true
        show-sql: true
